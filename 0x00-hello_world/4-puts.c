#include <stdio.h>
/**
 * main - A programming that prints a line with puts funtion
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts(Programming is like building a multilingual puzzle);
return (0);
}
